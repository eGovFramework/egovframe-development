import postcss from 'postcss';
import tsModule from 'typescript/lib/tsserverlibrary';
import { Options } from '../options';
import { Logger } from './logger';
export declare const getDtsSnapshot: (ts: typeof tsModule, processor: postcss.Processor, fileName: string, scriptSnapshot: tsModule.IScriptSnapshot, options: Options, logger: Logger) => tsModule.IScriptSnapshot;
