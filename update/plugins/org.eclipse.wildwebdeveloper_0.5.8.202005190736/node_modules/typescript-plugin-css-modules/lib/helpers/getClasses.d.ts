import postcss from 'postcss';
import { Logger } from './logger';
import { Options } from '../options';
export declare const enum FileTypes {
    css = "css",
    less = "less",
    scss = "scss"
}
export declare const getFileType: (fileName: string) => FileTypes;
export declare const getClasses: (processor: postcss.Processor, css: string, fileName: string, options: Options, logger: Logger) => import("icss-utils").CSSExports;
