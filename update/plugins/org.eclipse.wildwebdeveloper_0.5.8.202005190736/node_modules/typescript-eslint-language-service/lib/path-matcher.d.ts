export interface PathMatcher {
    notContains(filepath: string): boolean;
}
export declare type ESLintIgnoredPathsMatcherOptions = {
    projectDir: string;
};
export declare class ESLintIgnoredPathsMatcher implements PathMatcher {
    private ignoredPaths;
    constructor({ projectDir }: ESLintIgnoredPathsMatcherOptions);
    notContains(filepath: string): boolean;
}
