<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

	<bean id="${txtServiceName}" class="${txtServiceClass}">
#if(${chkProperty})		
		<property name="${txtPropertyName}" value="${txtPropertyValue}"/>
#end		
	</bean>
	 
	<bean id="${txtJobName}"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="${txtServiceName}" />
		<property name="targetMethod" value="${txtServiceMethod}" />
		<property name="concurrent" value="${cboConcurrent}" />
	</bean>

</beans>